require 'spec_helper'

describe LaunchConfigurationCleaner do
  it 'has a version number' do
    expect(LaunchConfigurationCleaner::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
